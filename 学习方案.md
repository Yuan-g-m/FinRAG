# FinRAG 金融领域 RAG 项目 · 一周速成学习方案

前提：代码位于本项目根目录（下文路径均相对此根目录）。环境已部署并跑通，`MySQL` / `Redis` / `Milvus` / `DeepSeek` 均可用，CLI 可正常问答。速成目标：**7 天能独立讲清整个金融领域 RAG 系统的调用链路、能跑通问答、能说清 RAG 各环节为什么这么做。**

---

## Day 1 · 架构骨架 + 基础层（读代码为主）

**目标**：建立整体地图，不深入细节。

- 读根目录结构 + `app.py`（只看注释和路由，不细抠）。
- 读 `base/config.py`、`base/logger.py`、`base/__init__.py`。
- 理解 `sys.path` 注入技巧、`config` 单例、日志双输出。

**动手**：改 `config.ini` 一个参数（如检索阈值、模型名），观察是否生效。

**验收**：用一句话画出「入口 → mysql_qa → rag_qa → LLM」的关系。今天不用懂细节，要懂「谁调谁」。

---

## Day 2 · mysql_qa：词法问答链路（0.8 天）

**目标**：掌握「命中即返回」的快速路径。

- 读 `mysql_qa/retrieval/bm25_search.py` → `utils/preprocess.py` → `db/mysql_client.py` → `cache/redis_client.py`。
- 重点理解：BM25 分数 → softmax 归一化 → 阈值 `0.85` 判断 `need_rag`；Redis 缓存的一二级读取。

**动手**：断点或打印，观察一次命中/未命中；调低阈值看更多答案直接返回。

**验收**：能解释「BM25 和向量检索的区别」「为什么先用 BM25 挡一下」。

---

## Day 3 · 向量库与知识库构建（1 天）

**目标**：掌握「文档如何变成向量、如何被检索」。

- 读 `rag_qa/core/vector_store.py`（重点：Milvus 集合、BGE-M3 稠密+稀疏向量、父块/子块、混合检索+重排）。
- 读 `rag_qa/core/document_processor.py` + `rag_qa/finance_text_spliter/`（父块/子块切分）与 `finance_document_loaders/`（OCR 加载）。
- 了解 `rag_qa/models/` 里 `bge-m3`、`bge-reranker-large` 用途。

**动手**：跑入库模式 `python -m rag_qa.main --data-processing`，看 `logs/app.log` 中「处理目录/添加了多少文档块」。

**验收**：能讲清「父块 1200 / 子块 300 / 重叠 50」为什么这么设，以及「用子块检索、为什么要回父块」。

---

## Day 4 · RAG 核心生成链路（1.2 天，最重要）

**目标**：掌握「分类 → 选策略 → 检索 → 生成」主流程。

- 读 `rag_qa/core/prompts.py`（4 类 Prompt）。
- 读 `rag_qa/core/query_classifier.py`（BERT 二分类：通用知识/专业咨询）。
- 读 `rag_qa/core/strategy_selector.py`（LLM 选 4 种策略）。
- 读 `rag_qa/core/new_rag_system.py` 的 `generate_answer` + `retrieve_and_merge`（重点）。

**动手**：断点一个「专业咨询」问题，依次看：分类结果→策略→检索文档数→最终 Prompt→答案；再试一个「通用知识」问题对比。

**验收**：能背出 4 种策略（直接检索/HyDE/子查询/回溯）的适用场景，并解释「通用知识为何不检索」。

---

## Day 5 · 集成编排 + Web 服务（1 天）

**目标**：把前面两套系统拼起来，并理解线上形态。

- 读 `new_main.py` 的 `query`、`call_llm`、会话历史增删（重点）。
- 对比 `legacy_backup/old_main.py` / `legacy_backup/rag_qa_core/rag_system.py`（V1）与 `new_main.py` / `new_rag_system.py`（V2），理解 V1→V2 加了什么（历史 + 流式）。
- 读 `app.py`：FastAPI 路由、WebSocket 流式、CORS、静态页、健康检查。

**动手**：启动 Web 服务、浏览器访问首页，用 `curl`/Swagger 调 `/query`，观察流式返回；走一遍带 `session_id` 的多轮对话。

**验收**：能画出「浏览器 → app.py → IntegratedQASystem → mysql_qa / rag_qa → LLM → 前端流式渲染」完整链路。

---

## Day 6 · 进阶与查漏（0.8 天）

**目标**：补齐细节，应对追问。

- 细读 `HyDE`、`子查询`、`回溯问题` 三个分支实现。
- 看 `rag_qa/rag_assessment/rag_as.py`（如何评估 RAG 质量）。
- 看 `static/` 前端与 `models/` 模型清单。
- 弄懂剩余为什么：为什么重排？为什么用 BGE-M3 双路向量？为什么会话只留 5 轮？

**验收**：把「为什么」逐一写成自己的答案。

---

## Day 7 · 复盘输出（0.7 天）

**目标**：把 7 天内容固化成自己的知识。

- 不看代码，手绘一张完整调用链图（建议 drawio/纸笔）。
- 写一份 1 页「项目介绍」，含：功能、架构、数据流、关键组件、亮点。
- 模拟面试自测：随机被问「如果知识库新增文档怎么做」「检索不准怎么办」「为什么先 BM25 再 RAG」，能答出来。

**验收**：能对着图，从「用户输入」讲到「答案输出」，不卡壳。

---

## 速成心法（牢记）

1. **追一条线**：选一个问题，从入口断点跟到底，比通读有效 10 倍。
2. **读注释块**：代码注释是教学式 `# todo` 分块，先看每块「输入→输出」，别逐行背。
3. **每模块一句总结**：读完写一句「它解决什么 + 输入 + 输出」，串成主线。
4. **先跑后读**：改动一个参数、跑一次入库、看一次日志，胜过看 100 行注释。
5. **抓大放小**：`finance_document_loaders/`、`rag_assessment/`、前端属于「背景」，先扫过，别陷进去；核心在 `new_rag_system.py` + `vector_store.py` + `new_main.py`。

## 每日时间建议

- Day 1：约 3–4h；Day 2：4–5h；Day 3：4–5h；Day 4：6–7h；Day 5：5–6h；Day 6：4h；Day 7：3h。总投入约 30–35h，一周可松紧调配。
